package api

import (
	"os"
	"testing"

	"github.com/gin-gonic/gin"
)

func TestMain(m *testing.M) {
	gin.SetMode(gin.TestMode) // set gin to test mode to avoid repeated test logs
	os.Exit(m.Run())
}
